const Button = ({ text }) => {
    return (

        <button className='px-4 py-2 rounded-xl text-primary-color font-semibold hover:scale-105 tansition duration-300 ease-in bg-accent-color'>{text}</button>

    )
}

export default Button